# Copy this file to .env and fill in your values.
# .env is gitignored – never commit credentials.

# Supabase direct database connection (Settings > Database > Connection string)
# Use the "Direct connection" (port 5432), not the pooler, for DDL migrations.
FLYWAY_URL=jdbc:postgresql://db.rkoaunvljkiorductfsb.supabase.co:5432/postgres
FLYWAY_USER=postgres
FLYWAY_PASSWORD=your_supabase_db_password_here
